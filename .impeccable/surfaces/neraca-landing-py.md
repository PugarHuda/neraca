---
version: 1
slug: "neraca-landing-py"
primary_target: "neraca/landing.py"
related_targets: []
---

# Surface brief: NERACA landing page (`GET /`, `GET /quote?addr=`)

Scope: the bureau's front door. Visitor mode: Persuade — an agent operator (or a judge) must understand in seconds what NERACA is, believe it because the page proves it with live memory and on-chain receipts, and act: ask the tariff for an address, open the registry, or wire their agent to the bureau. Audience/job: operators of agents that hire or get hired on ACP; judges. Proof/content on hand: live counts from memory, real Basescan transactions, the ACP directory names, the public price policy. Constraints: single Python-rendered template (`neraca/landing.py`), no JavaScript, one real HTML form (GET) that returns the memory-set quote, no fabricated claims or testimonials.

## Direction contract

THESIS: the bureau's counter ticket — take a number, name a counterparty, and the counter prints what its memory is worth. It refuses the SaaS hero (headline, subhead, two buttons, logo row) and the feature-card grid; the ticket is the hero and the form is the product.

OWN-WORLD: the established ledger world (eye-ease ledger-green paper, iron ink, hairlines at one device pixel, no radius, Courier Prime figures, Source Sans 3 apparatus, the fixed status colors) plus one committed material for this surface: the ticket is printed on canary carbon-copy stock (saturated yellow, a real ledger-office paper), with a perforated edge, and it carries 30–40% of the first viewport. Counters are numbered ruled forms; receipts are a struck tabulation.

STORY: the visitor understands that agents hire each other and nothing remembers who burned whom — except this bureau; believes it because the ticket answers with a real memory-priced quote and the receipts are real transactions; and does one thing: submits an address, or opens the registry, or copies the MCP/x402/ERC-8004 counter instructions.

FIRST VIEWPORT: left, the canary ticket at full height of the viewport on desktop: NERACA masthead, NOW SERVING and the live verdict count as the ticket number, the form (address field in mono, one submit "Ask the tariff"), and — when the ticket has been submitted — the stamped answer (name if known, events remembered, price, how to buy the verdict). Right, the thesis in three short lines of bureau voice, then the three counters (01 x402, 02 MCP, 03 ERC-8004) as ruled forms with one instruction each. The primary action is the form's submit, inside the ticket.

FORM: the counter ticket with a real quote form — candidate 3 of my second-round ordered list (1 passbook, 2 stamp sheet, 3 counter ticket, 4 T-account, 5 receipt roll, 6 index tabs, 7 telegram); seed key be2eba4e (re-roll round 1). Signature interaction: submitting the form reprints the same ticket, now stamped with the quote — no JavaScript, the page is the receipt.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance.

Unresolved: none.
